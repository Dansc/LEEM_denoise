# Denoising LEEM images

Example of using a UNet structure to denoise LEEM images.
Images are added with random noise, targets are unmodified images.

## Getting Started

Runs are stored for tensorboard in /runs
